<% custom_column_class = "col-12 !important" %>
<%# This line should override the global container div's column settings in application.html.erb  %>

<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<%= button_to(
        "Log Out",
        destroy_user_session_path,
        method: :delete
      ) %>
